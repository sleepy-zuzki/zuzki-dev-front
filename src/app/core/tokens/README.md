# Tokens de Inyección

Este directorio contiene los `InjectionToken` de Angular.

Los tokens de inyección son una herramienta que permite inyectar valores que no son clases, como objetos de configuración, strings o funciones. Se utilizan para desacoplar el código de valores concretos y facilitar las pruebas y la configuración.
